# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: accessibility\loginPage.a11y.spec.ts >> Login Page - Accessibility @accessibility >> login page is accessible and loads correctly @accessibility @a11y
- Location: tests\accessibility\loginPage.a11y.spec.ts:4:7

# Error details

```
Error: expect(page).toHaveTitle(expected) failed

Expected pattern: /OrangeHRM/
Received string:  ""
Timeout: 10000ms

Call log:
  - Expect "toHaveTitle" with timeout 10000ms
    23 × unexpected value ""

```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test.describe("Login Page - Accessibility @accessibility", () => {
  4  |   test("login page is accessible and loads correctly @accessibility @a11y", async ({ page }) => {
  5  |     // Verify page title
> 6  |     await expect(page).toHaveTitle(/OrangeHRM/);
     |                        ^ Error: expect(page).toHaveTitle(expected) failed
  7  | 
  8  |     // Verify all form inputs have visible labels
  9  |     await expect(page.getByPlaceholder("Username")).toBeVisible();
  10 |     await expect(page.getByPlaceholder("Password")).toBeVisible();
  11 | 
  12 |     // Verify login button is visible
  13 |     await expect(page.getByRole("button", { name: "Login" })).toBeVisible();
  14 |   });
  15 | });
  16 | 
```